syntax = "proto3";

package shmem;

service ShmemRPC {
    rpc CheckReady(EpochIndex) returns (Reply);
}

message EpochIndex {
    bool index = 1;
    int32 rank = 2;
}

message Reply {
    bool ready = 1;
    int64 data_base = 2;
    int64 meta_base = 3;
}
